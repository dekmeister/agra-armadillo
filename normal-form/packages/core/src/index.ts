// @normal-form/core — the game's deterministic, headless truth (no DOM, no RNG,
// no wall-clock). S1 ships the policed message catalog; the sim, validator,
// machine, and level runtime land in later sessions.
export * from "./messages/index.ts";
