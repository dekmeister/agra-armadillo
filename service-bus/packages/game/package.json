{
  "name": "@service-bus/game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Service Bus Svelte + SVG mission console (Direction B — Minimalist), wired to the deterministic core.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@service-bus/core": "*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "vite": "^7.3.5"
  }
}
