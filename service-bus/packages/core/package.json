{
  "name": "@service-bus/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Service Bus deterministic, headless A-GRA DMS simulation core (no DOM, no wall-clock; pure tick(state) -> state').",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  }
}
